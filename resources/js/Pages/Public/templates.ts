import DefaultTemplate from './Templates/Default';

const templates = {
    default: {
        name: 'Default',
        type: 'free',
        component: DefaultTemplate,
    },
};

export default templates;
